import { GoogleGenerativeAI } from '@google/generative-ai'; import dotenv from 'dotenv'; dotenv.config(); const genAI = new GoogleGenerativeAI(process.env.GOOGLE_AI_KEY!); (async () => { try { const model = genAI.getGenerativeModel({ model: 'text-embedding-004' }); await model.embedContent('hello'); console.log('004 works!'); } catch(e) { console.error('004 failed:', e.message); } try { const model = genAI.getGenerativeModel({ model: 'embedding-001' }); await model.embedContent('hello'); console.log('001 works!'); } catch(e) { console.error('001 failed:', e.message); } process.exit(0); })();